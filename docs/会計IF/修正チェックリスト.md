# 会計IF 修正チェックリスト（初回ロジック）

レビューで検出した要修正点。修正後に全体を再エクスポートいただければ再確認します。
根拠: [パラメータ受け渡し検証.md](パラメータ受け渡し検証.md) / [必要ロジック抽出.md](必要ロジック抽出.md) / [テーブル・エンティティ突合.md](テーブル・エンティティ突合.md)

## 🔴 必須（不具合）

### 1. 仕掛品出庫「更新」フロー（仕掛品出庫入力データ更新）
- [ ] **UPDATE文が構文エラー**。SET句のカンマ抜け2箇所＋WHERE直前の余分カンマ。下記に修正。
- [ ] 更新の呼び出しが **HTTP GET** → **PUT（または POST）** に変更（BloomMaker側のメソッドとルート）。
- [ ] （確認）WHERE の列名が `company_id`。他テーブルは `company_identifier`。実列名を確認。
- [ ] （確認）`recorded_date` 列に `/*recording_date*/` を束縛（列名と変数名の不一致）。変数マッピングが通っているか確認。

**修正前**
```sql
SET
issue_serial_number = /*issue_serial_number*/0,
recorded_date = /*recording_date*/''
issue_date = /*issue_date*/''
management_number = /*management_number*/0,
...
issue_destination = /*issue_destination*/0,
WHERE ...
```
**修正後**
```sql
UPDATE tmh_acif_work_in_progress_issue_quantities
SET
  issue_serial_number  = /*issue_serial_number*/0,
  recorded_date        = /*recording_date*/'',      -- ←カンマ追加
  issue_date           = /*issue_date*/'',           -- ←カンマ追加
  management_number    = /*management_number*/0,
  parts_number         = /*parts_number*/0,
  quantity             = /*quantity*/0,
  pre_issue_department = /*pre_issue_department*/0,
  issue_destination    = /*issue_destination*/0      -- ←末尾カンマ削除
WHERE
  company_id = /*company_id*/''
  AND issue_serial_number = /*issue_serial_number*/''
```

### 2. TMC買掛金不照合追加「チェック」
- [ ] チェック工程の呼び先が **「TMC売掛金不照合追加入力データチェック」**（別ドメイン=売掛金）。**買掛金用のチェックフロー**に差し替え。
- [ ] 売掛金チェックの入力項目名は別体系（partner_id / shukka_date / tmckenshu_date / correctreason_type / recording_date / gogutihokyu_type …）で、画面の送信名（trading_partner_identifier / issuance_date / tmc_delivery_date / correct_reason_type / recorded_date …）と噛み合わない。買掛金用に項目名を揃える。
- [ ] （補足）登録フロー「TMC買掛金不照合追加入力データ登録」は正常（12項目一致）。登録ルート名が `.../check` になっている点は実体INSERTと紛らわしいので命名見直し推奨。
- [ ] 画面側の入力検証（validationRules）が0件。追加系として必要な検証を検討。

### 3. 部品仕入単価「検索」フロー（部品仕入単価マスタ一覧取得）
- [ ] 入力定義が **部品売上単価の流用**。実SQLに合わせて修正:
  - 追加宣言: `supplier_identifier` / `issue_destination` / `unit_price_from` / `unit_price_to` / `configuration_date_from` / `configuration_date_to`
  - 削除: `trading_partner_identifier` / `combined_supply_type` / `external_sale_type`（SQL未使用の死んだ定義）
- [ ] 修正後、BloomMakerの仕入先/出荷先/単価/設定日フィルタが効くことを確認。

### 4. 【系統的】日付フォーマット不一致（全フロー）
パラメータ＝`yyyy/MM/dd` に対し、データ＝`yyyyMMdd`。SQLは全て**生の文字列比較**で正規化しておらず、
**日付の範囲検索・一致条件が効かない**。1フローではなく会計IF全体の問題。

**影響フロー（日付比較あり・すべて生比較）**
- 検索: 部品仕入単価 / 部品売上単価（適用開始・終了日）、TMC買掛金（発行日・計上日・TMC納品日）、
  仕掛品出庫（計上日・出庫日）、部品仕入返品（受入日）、資材差額調整（発行日・計上日・納品日）、
  特定工数（計上日）、加工不良（計上日）
- チェック/更新/削除の日付=一致条件（recorded_date / delivery_date / shukka_date 等）

**修正方針（いずれか）**
- [ ] **推奨: 入口で正規化** — 画面(BloomMaker)の日付を送信時に `yyyyMMdd` へ変換（区切り除去）。
  または各フローのSQL前マッピングで変換。全フロー一貫で最も安全。
- [ ] **SQLで正規化** — 各日付バインドを `REPLACE(...,'/','')` で区切り除去（列側は素のまま＝索引維持）。

**例（仕掛品出庫 検索）**
```sql
/*IF recorded_date_from != null*/
AND p.recorded_date >= REPLACE(/*recorded_date_from*/'', '/', '')
/*END*/
/*IF recorded_date_to != null*/
AND p.recorded_date <= REPLACE(/*recorded_date_to*/'', '/', '')
/*END*/
-- issue_date_from / issue_date_to も同様
```
- [ ] （確認）日付列の型。`yyyyMMdd` の文字列(varchar)前提なら上記でOK。DATE型ならTO_DATE等で両辺を揃える。
- [ ] （ついで）仕掛品出庫 検索の `AND p.quantity > 0`（`/*IF quantity != null*/`配下だが入力値を使わず常に>0）と、
  `p.parts_number = ...`（他はLIKE、ここだけ完全一致）の意図を確認。

## 🟡 確認・整合

- [ ] **特調たな卸単価未決「検索」**: `company_identifier` が入力定義にあるが画面は未送信。サーバ解決か、SQLで会社IDを使うなら供給要否を確認。
- [ ] **資材差額調整「削除」**: WHERE が `issue_serial_number` 単独。会社跨ぎ削除防止のため `company_identifier` 等の複合キーにすべきか確認（他の削除は複合キー）。
- [ ] **部品仕入単価**: 削除（delete）フローが無い。必要なら追加。
- [ ] **ルート命名の統一**: ULID系（`01J.../getList`）と名前系（`tmh_.../check|new|edit|delete`）が混在。名前系に統一推奨。
- [ ] **部品仕入単価のルート単複**: `..._price/check`・`..._price/new`（単数）と `..._prices`・`..._prices/edit`（複数）が混在。統一推奨。

## 🗄 データ定義（別軸・DB担当と要調整）

- [ ] **DDLスクリプトが旧名**（`specific_effort`↔`specific_labor_hours`、`_quantity`↔`_quantities` 等）。entity/フローと不一致。entity準拠で `会計IF_テーブルDDL_ULID版.sql` を再生成。
- [ ] **書込3テーブルが xlsx のみ定義**（`processing_defect_quantities` / `specific_labor_hours` / `work_in_progress_issue_quantities`）。entity export・DDL に無い。DB構築の正典化。

## 🆕 新規CRUD作成（別トラック・テーブル情報待ち）
[新規CRUD設計.md](新規CRUD設計.md) 参照。部品売上単価ほか計7画面。対象テーブル確定後に着手。
